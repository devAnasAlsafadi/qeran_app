/// Snackbar tone. [notice] is the calm "heads-up" channel (soft cream
/// surface + wine text/icon) for minor info that should NOT shout; [error]
/// stays the loud danger banner for serious failures that must be noticed.
enum SnackBarType { success, error, info, notice }
