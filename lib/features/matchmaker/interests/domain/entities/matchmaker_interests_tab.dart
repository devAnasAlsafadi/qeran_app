/// The three read-only tabs of the matchmaker interests mirror, in display
/// order: active matches, likes the viewed user received, likes they sent.
/// The index doubles as the segmented-tabs / IndexedStack index.
enum MatchmakerInterestsTab { matches, incoming, outgoing }
